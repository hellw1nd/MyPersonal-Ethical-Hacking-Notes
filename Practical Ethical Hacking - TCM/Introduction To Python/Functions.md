```python
#FUNCTIONS

def who_am_i(): #this is a function without parameter and indentation are one of the important in python when declaring a function
	name = "mark"
	age = 21;
	print("my name is" + name + "I am " + str(age) + "years of age.")
who_am_i()
print(age)

def add_one_hundred(num) :
	print(num + 100)
	
add_one_hundred(100) 

def multiply(x,y):
	return x*y

multiply(7,7)
print(multiply(7,7))

def square_root(x):
	print(x ** .5)
square_root(64)

def nl():
	print("\n")
nl()
```